header
    nav.navbar.navbar-expand-sm.navbar-dark.bg-dark
        a.navbar-brand(href='/') Cache-Cache
        button.navbar-toggler(type='button' data-toggle='collapse' data-target='#navbarSupportedContent' aria-controls='navbarSupportedContent' aria-expanded='false' aria-label='Toggle navigation')
            span.navbar-toggler-icon
        #navbarSupportedContent.collapse.navbar-collapse
            ul.navbar-nav.mr-auto
                li.nav-item
                    a.nav-link(href='/')
                        | Accueil
                li.nav-item
                    a.nav-link(href='/apropos') 
                        | Le Jeu
            form.form-inline.my-2.my-lg-0
                if pageTitle === "Hall des Joueurs" || (pageTitle === "A Propos" && user) || pageTitle === "Profil"
                    a.nav-link.disabled(href='#' tabindex='-1' aria-disabled='true') Content de vous voir
                    a.nav-link.playerLink(href="/profil/" + user.name) #{user.name} 
                    a.btn.btn-outline-secondary.my-2.my-sm-0.mr-md-3(href='/logout') Deconnecter
                if pageTitle === "Game"
                    a.nav-link.disabled(href='#' tabindex='-1' aria-disabled='true') Game: #{myGamename} | Temps de jeux: 
                        span#time 00:00
                if pageTitle === "Game"
                    a.btn.btn-danger.my-2.my-sm-0.mr-md-3(href='/hall') Sortir du Jeu