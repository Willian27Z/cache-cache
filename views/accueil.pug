extends partials/layout

block main
    .jumbotron
        h1.display-4 Bienvenu à Cache-Cache!
        p.lead
            | Pour jouer ou regarder des matchs, veuillez vous connecter ou s'inscrire. Ne t'inquiète pas, ça ne fait pas mal!
    if message.type === "error"
        .container-fluid
            .alert.alert-danger.mt-md-3(role='alert') #{message.text}
    .row
        .col-6
            #loginBox.border.border-dark.rounded
                h1.display-4 Se Connecter
                hr
                form(action='/login' method='GET' name='loginForm')
                    .form-group
                        label Identifiant (nom de joueur)
                            input#identifiant.form-control(type='text' name='identifiant' required)
                    .form-group
                        label Mot de Passe
                            input#mdp.form-control(type='password' name='mdp' required)
                    button.btn.btn-dark(type='submit') Se connecter
        .col-6
            #loginBox.border.border-dark.rounded
                h1.display-4 S'Inscrire
                hr
                form(action='/signup' method='GET' name='signupForm')
                    .form-group
                        label Identifiant (nom de joueur)
                            input#identifiant.form-control(type='text' name='identifiant' maxlength="24" required)
                    .form-group
                        label E-mail
                            input#identifiant.form-control(type='email' name='email' required)
                    .form-group
                        label Mot de Passe
                            input#mdp.form-control(type='password' name='mdp' placeholder='5 caractères minimum' minlength="5" required)
                    button.btn.btn-dark(type='submit') S&apos;inscrire